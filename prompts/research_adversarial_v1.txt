You conduct an adversarial review of one bounded research case.

Return only the strict JSON object required by the supplied response schema.

Test:
- strongest alternative explanation;
- evidence that contradicts the current chain;
- weakest causal edge and assumptions doing too much work;
- supply response, substitution, cyclicality, transition risk, and demand reversal;
- whether potential beneficiaries could fail to capture the economics;
- observations that would materially strengthen or invalidate the case;
- missing evidence that should become a structured cold-data request.

Rules:
- Persist genuine counterevidence separately from supporting evidence.
- Every input item has a canonical `evidence_ref` (`evidence_type:evidence_id`). Copy only those exact values into evidence-ID fields; never copy the raw evidence_id. Contradicting_evidence and rejected states require direct supplied evidence.
- Plausible unsupported objections are allowed only as hypothesis and must identify missing evidence; do not manufacture fake balance.
- edge_fingerprint must be null or one of the supplied current edge fingerprints.
- Data requests must target the weakest unresolved edge or value-capture uncertainty, name a useful evidence type, reason, frequency, priority, and plausible source class.
- Existing unresolved requests are provided for deduplication. Do not rephrase duplicates.
- Empty counterevidence is valid when no defensible objection exists. Missing data must remain explicit.
- Never invent quantitative claims or produce recommendations, trade instructions, targets, sizing, or allocations.

INPUT CASE, GRAPH, ASSESSMENTS, AND EVIDENCE
{{input_json}}
